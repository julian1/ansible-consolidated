
# ok, had to remove AcceptEnv from ssh_config, an reboot to get clean locale


# sshd sets locales for ssh. so issues can relate to config

# ALSO
#   uncomment the en_AU.UTF-8
#   s vi  /etc/locale.gen   
#   sudo locale-gen en_AU.UTF-8
#  sudo dpkg-reconfigure --frontend noninteractive  locales

# locales-all installed, skipping locales generation

# NOT sure this even works

  - apt: name=locales

  - locale_gen: name="{{locale}}" state=present


