
  # SHOULD PROBABLY RENAME TO PATHGEN

  # should make pathogen plugins independent of just vimrc

  # TODO factor out the hard-coded user directory
  # vimrc requires the appropriate assocation to be set eg, #  au BufNewFile,BufRead *.sol setf solidity


  # .vimrc
  # TODO maybe templatize
  # - copy: src=vimrc dest={{vim_path}}/.vimrc



  # pathogen
  # https://github.com/tpope/vim-pathogen
  - get_url: url='https://tpo.pe/pathogen.vim'                dest={{vim_path}}/.vim/autoload/pathogen.vim

  - apt: name=git

  # solidity, sol
  - git: repo=https://github.com/tomlion/vim-solidity         dest={{vim_path}}/.vim/bundle/vim-solidity

  # rust, rs
  - git: repo=https://github.com/rust-lang/rust.vim           dest={{vim_path}}/.vim/bundle/rust

  # coquille, v
  - git: repo=https://github.com/the-lambda-church/coquille   dest={{vim_path}}/.vim/bundle/coquille

  # purescript
  - git: repo=https://github.com/raichoo/purescript-vim.git   dest={{vim_path}}/.vim/bundle/purescript

  # haskell - just use the built in one,

  # recursive... ansible git is recursive by default,
  # git clone --recursive https://github.com/sdiehl/haskell-vim-proto.git

  # the dammn indirection is wrong...
  # - git: repo=https://github.com/sdiehl/haskell-vim-proto.git  dest={{vim_path}}/.vim/bundle/haskell-vim-proto


  - git: repo=https://github.com/idris-hackers/idris-vim.git  dest={{vim_path}}/.vim/bundle/idris


