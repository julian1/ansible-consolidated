- hosts: chromebox
  vars:
    - mirror: http://mirror.internode.on.net/pub/debian
    - version: testing
    - upgrade: safe
    - timezone: Australia/Hobart
    - locale: en_AU.UTF-8
  vars_files:
    - credentials.yml
  roles:
    - debian
    - timezone
    - locale
    - meteo

  tasks:
    - apt: name=pciutils        # lspci

