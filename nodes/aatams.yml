
- hosts:
    - aatams
  vars_files:
    - ../vars/jessie.yml
    - ../vars/credentials.yml
  vars:
    - users:
      - { name: meteo, group: adm, pubkey: "{{meteo_pubkey}}" }
    - aatams:
      - war: /home/meteo/imos/aatams/target/aatams-3.16.4.war
  roles:
    - debian
    - timezone
    - locale
    - common
    - meteo-dotfiles
    - postgres
    - tomcat-port-forward
    - aatams

