
- hosts:
    - aatams.localnet
  vars_files:
    - ../vars/jessie.yml
    - ../vars/credentials.yml
  vars:
    - users:
      - { name: meteo, group: adm, pubkey: "{{meteo_pubkey}}" }
  roles:
    - debian
    - timezone
    - locale
    - common
    - meteo-dotfiles
    - postgres
    - tomcat-port-forward
    - aatams

    # TODO Remove this, should be a separate task,
    # - aatams:
    #  - war: /home/meteo/imos/aatams/target/aatams-3.16.4.war


